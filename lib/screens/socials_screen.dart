import 'package:flutter/material.dart';

class SocialsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Socials")),
      body: Center(child: Text("Follow us on our social media.")),
    );
  }
}
